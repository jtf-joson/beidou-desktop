{
  "name": "@beidou/core",
  "version": "0.7.0",
  "private": true,
  "description": "北斗work 核心能力包:语义/口径编译/护栏/诊断/审计/认证(零平台依赖红线,见 docs/CODE_STANDARDS)",
  "main": "src/index.ts",
  "dependencies": {
    "yaml": "2.5.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "2.1.9",
    "vitest": "2.1.9"
  }
}