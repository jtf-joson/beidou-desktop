# DSH Desktop Release Notes

本文件是发布说明的风格参考。CI 在生成 GitHub Release 正文时读取前 120 行作为 `<style-reference>`。手工维护，无需每次发布都更新。

写作约定：

- 首行是 `# DSH Desktop v<版本> — <一句话主题>`。
- 使用这些二级标题归纳改动：`## 更新内容`、`## 问题修复`、`## 性能与优化`、`## 升级说明`、`## 说明`。
- 全面覆盖本次版本的所有实质改动点（包括功能特性、交互体验、缺陷修复、性能调优、平台系统适配等）。
- 每个改动点用清晰的列表条目（`- **模块/功能**：说明改动表现及解决的问题`）阐述，说清技术变化及给用户或运维带来的实际收益。
- 不逐条机械复述无意义的 Git commit，不放任何链接。

---

# DSH Desktop v0.0.0 — 示例主题

## 更新内容

- **会话与工作区**：描述新增能力与改进，说清变化本身以及给用户带来的好处。
- **界面交互体验**：说明界面布局、菜单或主题适配的改动点。

## 问题修复

- **系统启动与进程管理**：修复特定平台（如 Windows 权限或软链接）下的启动异常，提升应用启动成功率。
- **插件兼容性与加载**：修复插件在特定运行时版本下导出缺失或依赖未解析的故障。

## 性能与优化

- **包体与构建体积**：优化打包产物体积，剔除冗余依赖，加快启动加载。

## 说明

- 可在客户端内「检查更新」直接更新到该版本。
